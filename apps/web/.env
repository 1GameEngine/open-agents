# Local one-command dev — committed for self-hosted / PGlite workflow.
# Replace AI_GATEWAY_API_KEY with a real key from https://vercel.com/dashboard/ai-gateway

# ─── Database (PGlite: bun run dev:pglite listens on 5433) ───────────────────
DATABASE_URL="postgres://postgres:postgres@127.0.0.1:5433/postgres"
POSTGRES_URL="postgres://postgres:postgres@127.0.0.1:5433/postgres"
POSTGRES_POOL_MAX=1

# ─── Workflow (local file world — does not share PGlite connection) ─────────
WORKFLOW_TARGET_WORLD="local"
WORKFLOW_LOCAL_DATA_DIR="/tmp/open-agents-workflow-local"

# ─── Crypto (dev-only values in repo — rotate if you expose this deploy) ───
JWE_SECRET="2Lp_JtGLAUQQ78is4SJh2PEn0UdlAGSpoBlGKPl4J1A"
ENCRYPTION_KEY="144f59373047778583331cbf6c62c5b7796c335f2f3b848a327ad709bae96ab4"

# ─── AI Gateway ─────────────────────────────────────────────────────────────
AI_GATEWAY_API_KEY=REPLACE_WITH_VERCEL_AI_GATEWAY_KEY

# ─── Browser auth (must match BOOTSTRAP_API_KEY after first bootstrap) ──────
NEXT_PUBLIC_SELF_HOSTED_API_KEY="oha_devlocal_openharness_default_key_change_me"

# ─── Bootstrap: deterministic initial API key (first run only) ─────────────
BOOTSTRAP_API_KEY="oha_devlocal_openharness_default_key_change_me"

# ─── Sandbox (local-fs session roots) ───────────────────────────────────────
SANDBOX_ROOT_DIR="/tmp/open-agents-sandboxes"
