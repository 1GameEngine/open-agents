# Git-tracked production overlay (non-secret differences only).
# Replace placeholders in deployment platform env vars instead of committing secrets.
WORKFLOW_TARGET_WORLD="postgres"
POSTGRES_POOL_MAX=20
APP_URL="https://agents.1game.design"

# ─── 1game SSO (production values) ──────────────────────────────────────────
MBBS_API_BASE_URL="https://api.1game.design/main"
NEXT_PUBLIC_BBS_BASE_URL="https://bbs.1game.design"
NEXT_PUBLIC_AGENTS_SSO_BASE_URL="https://agents.1game.design/api/auth/sso"
