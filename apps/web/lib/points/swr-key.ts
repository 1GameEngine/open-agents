/** SWR cache key for GET /api/points/balance — use with `mutate()` after usage changes. */
export const POINTS_BALANCE_SWR_KEY = "/api/points/balance";

/** Base path for GET /api/points/transactions (append ?offset= for pagination). */
export const POINTS_TRANSACTIONS_API_PATH = "/api/points/transactions";
