# Local development secret overrides (NOT committed).
# Copy to `apps/web/.env.dev.local` when you need dev-only secrets.
#
# Load order:
# `.env` -> `.env.dev` -> `.env.local` -> `.env.dev.local`

# Example:
# AI_GATEWAY_API_KEY=your-dev-gateway-key
