# Local production-secret override (DO NOT COMMIT `.env.prod.local`).
#   cp apps/web/.env.prod.local.example apps/web/.env.prod.local
#
# Loaded after `.env` + `.env.prod`; same keys here win last.

# AI_GATEWAY_API_KEY=vck_xxx
# JWE_SECRET=replace-with-prod-secret
# ENCRYPTION_KEY=replace-with-prod-hex-key
# GITHUB_CLIENT_SECRET=...
