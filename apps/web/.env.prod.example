# Overlay template for `apps/web/.env.prod`.
# Keep only non-secret production differences relative to `.env`.

# Example: production should use postgres world and larger DB pool.
WORKFLOW_TARGET_WORLD="postgres"
POSTGRES_POOL_MAX=20

# Example: canonical app URL used in metadata/callback behavior.
APP_URL="https://your-domain.example.com"
