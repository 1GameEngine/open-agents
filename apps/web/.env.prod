# Git-tracked production overlay (non-secret differences only).
# Replace placeholders in deployment platform env vars instead of committing secrets.
WORKFLOW_TARGET_WORLD="postgres"
POSTGRES_POOL_MAX=20
APP_URL="https://your-domain.example.com"
