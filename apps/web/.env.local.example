# Local secret overrides (not committed).
#   cp apps/web/.env.local.example apps/web/.env.local
#
# Load order:
# 1) .env
# 2) .env.dev or .env.prod (based on NODE_ENV)
# 3) .env.local
# 4) .env.dev.local or .env.prod.local
#
# Put secrets here.
AI_GATEWAY_API_KEY=
# GITHUB_CLIENT_SECRET=
# GITHUB_APP_PRIVATE_KEY=
